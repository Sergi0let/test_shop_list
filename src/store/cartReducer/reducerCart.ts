const initialState = {
  cart: [],
  total: 0,
};
const reducerCart = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducerCart;
